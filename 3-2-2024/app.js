
// console.log(prompt())



































// // Using prompt to ask for user input
// let name = prompt("Please enter your name:");

// // Displaying a message using the entered name
// if (name !== null && name !== "") {
//     alert("Hello, " + name + "! Welcome to our website.");
// } else {
//     alert("Hello, guest! Welcome to our website.");
// }

// Function to calculate the area of a rectangle
// function calculateRectangleArea(length, width) {
//     return length * width;
// }

// // Using prompt to ask for user input for length and width
// let length = prompt("Enter the length of the rectangle:");
// let width = prompt("Enter the width of the rectangle:");

// // Parsing input strings to numbers
// length = parseFloat(length);
// width = parseFloat(width);

// // Checking if both inputs are valid numbers
// if (!isNaN(length) && !isNaN(width) && length > 0 && width > 0) {
//     // Calculating the area and displaying the result
//     let area = calculateRectangleArea(length, width);
//     alert("The area of the rectangle with length " + length + " and width " + width + " is: " + area);
// } else {
//     // Displaying an error message if inputs are invalid
//     alert("Invalid input. Please enter valid positive numbers for length and width.");
// }